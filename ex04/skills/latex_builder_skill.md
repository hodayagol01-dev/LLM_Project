# LaTeX Builder Skill

The LaTeX Builder agent is responsible for converting the generated content into a professional PDF using LuaLaTeX.

## Responsibilities:
- Managing the `main.tex` and `.cls` files.
- Handling compilation errors and warnings.
- Ensuring correct bibliography processing with Biber.
- Managing assets and images within the document.
